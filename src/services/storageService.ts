
import { storageService } from './storage';

// Re-export the storage service for backward compatibility
export { storageService };
export type { UploadOptions, UploadResult, UserImage } from './storage';
